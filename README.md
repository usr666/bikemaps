# bikemaps
Collection of bicycle maps

- Store `.gpx` files in [`maps/`](./maps/)
- Open [`index.html`](./index.html) via GitHub Pages to see all GPX files dynamically linked to gpx.studio
- The page updates automatically — no changes needed when new files are added to `maps/`
